# Cloudflare – retrieved via `wrangler whoami`
CLOUDFLARE_ACCOUNT_ID=

# Create at https://dash.cloudflare.com/profile/api-tokens
# Use template "Edit Cloudflare Workers" (needs Workers Scripts:Edit + Account:Read)
CLOUDFLARE_API_TOKEN=

# Neon – create project at https://console.neon.tech, then:
#   NEON_API_KEY   → Account Settings → API Keys
#   DATABASE_URL   → project connection string (postgres://...)
NEON_API_KEY=
DATABASE_URL=

# Hyperdrive – run after DATABASE_URL is set:
#   wrangler hyperdrive create contextgraph-db --connection-string "$DATABASE_URL"
# Then paste the returned ID here and in wrangler.jsonc
HYPERDRIVE_ID=

# ClickHouse (Cloud tier)
CLICKHOUSE_HOST=https://your-instance.clickhouse.cloud:8443
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=contextgraph
